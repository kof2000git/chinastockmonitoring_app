{
  "name": "china-stock-monitor-android-client",
  "version": "1.0.0",
  "private": false,
  "description": "A股涨停监控系统 Android 客户端（Capacitor WebView Shell）",
  "scripts": {
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "build:android:debug:windows": "npm run android:sync && cd android && gradlew.bat assembleDebug",
    "build:android:debug:unix": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
